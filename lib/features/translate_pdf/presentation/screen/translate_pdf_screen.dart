import 'package:flutter/material.dart';

class TranslatePdfScreen extends StatelessWidget {
  const TranslatePdfScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('Translate Pdf Screen'),
    );
  }
}
