import 'package:flutter/material.dart';

class WatermarkPdfScreen extends StatelessWidget {
  const WatermarkPdfScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('Watermark Pdf Screen'),
    );
  }
}
